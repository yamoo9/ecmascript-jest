/* shuffle 유틸리티 ------------------------------------------------------------- */

// const numbers = [2, 4, 6, 3, 5, 1];
// shuffle(numbers)  →  섞인 numbers의 항목 갯수는 6이다.
// shuffle(numbers)  →  섞인 numbers는 numbers의 항목 순서와 하나 이상 다르다.
